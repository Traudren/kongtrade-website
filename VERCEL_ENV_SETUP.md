# Настройка переменных окружения в Vercel

## Шаг 1: Войдите в Vercel Dashboard

1. Перейдите на https://vercel.com
2. Войдите в свой аккаунт (через GitHub)
3. Выберите ваш проект `kongtrade-website`

## Шаг 2: Откройте настройки переменных окружения

1. В верхней панели проекта нажмите на вкладку **Settings**
2. В левом меню выберите **Environment Variables**

## Шаг 3: Добавьте переменные

Для каждой переменной:

1. Нажмите кнопку **Add New** (или **Add**)
2. Введите **Key** (имя переменной)
3. Введите **Value** (значение переменной)
4. Выберите окружения, где будет использоваться переменная:
   - ✅ **Production** - для production деплоев
   - ✅ **Preview** - для preview деплоев (pull requests)
   - ✅ **Development** - для локальной разработки (опционально)
5. Нажмите **Save**

---

## Переменная 1: DATABASE_URL

### Если база данных УЖЕ настроена:

**Key:** `DATABASE_URL`

**Value:** Ваша строка подключения к PostgreSQL
```
postgresql://username:password@host:5432/database_name?schema=public
```

**Пример:**
```
postgresql://user:pass123@db.example.com:5432/kongtrade?schema=public
```

**Окружения:** ✅ Production, ✅ Preview

---

### Если база данных ЕЩЕ НЕ настроена:

**Вариант A: Создать Vercel Postgres (рекомендуется)**

1. В Vercel Dashboard → ваш проект → вкладка **Storage**
2. Нажмите **Create Database**
3. Выберите **Postgres**
4. Выберите план (начните с **Hobby** - бесплатный)
5. Выберите регион (ближайший к вам)
6. Нажмите **Create**
7. Vercel автоматически создаст переменную `POSTGRES_URL`
8. Добавьте алиас в Environment Variables:
   - **Key:** `DATABASE_URL`
   - **Value:** `$POSTGRES_URL` (или скопируйте значение из `POSTGRES_URL`)
   - **Окружения:** ✅ Production, ✅ Preview

**Вариант B: Использовать внешнюю базу данных**

#### Supabase (бесплатный план):
1. Перейдите на https://supabase.com
2. Создайте аккаунт и новый проект
3. В **Settings** → **Database** найдите **Connection string**
4. Скопируйте строку подключения (формат: `postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres`)
5. Замените `[YOUR-PASSWORD]` на ваш пароль
6. Добавьте в Vercel как `DATABASE_URL`

#### Neon (бесплатный план):
1. Перейдите на https://neon.tech
2. Создайте аккаунт и новый проект
3. Скопируйте **Connection string** из Dashboard
4. Добавьте в Vercel как `DATABASE_URL`

#### Railway ($5 кредитов/мес):
1. Перейдите на https://railway.app
2. Создайте новый проект → **New** → **Database** → **PostgreSQL**
3. Скопируйте **DATABASE_URL** из переменных окружения
4. Добавьте в Vercel как `DATABASE_URL`

---

## Переменная 2: NEXTAUTH_URL

### Для первого деплоя (временное значение):

**Key:** `NEXTAUTH_URL`

**Value:** `https://your-project-name.vercel.app`
(замените `your-project-name` на имя вашего проекта в Vercel)

**Пример:**
```
https://kongtrade-website.vercel.app
```

**Окружения:** ✅ Production, ✅ Preview

### После настройки кастомного домена:

1. После добавления домена `kongtrade.com` в Vercel
2. Обновите значение на:
   ```
   https://kongtrade.com
   ```
3. Или оставьте оба варианта (Vercel автоматически определит правильный)

---

## Переменная 3: NEXTAUTH_SECRET

### Генерация секретного ключа:

**Способ 1: Через командную строку (рекомендуется)**

```bash
openssl rand -base64 32
```

Скопируйте сгенерированную строку.

**Способ 2: Онлайн генератор**

1. Перейдите на https://generate-secret.vercel.app/32
2. Скопируйте сгенерированную строку

**Способ 3: Через Node.js**

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

### Добавление в Vercel:

**Key:** `NEXTAUTH_SECRET`

**Value:** Сгенерированная строка (например: `xK8pL2mN9qR5sT7vW3yZ6bC1dF4gH8jK0lM2nP5rS8tU1vW4xY7zA0bC3dE6f`)

**Окружения:** ✅ Production, ✅ Preview

**⚠️ ВАЖНО:** 
- Никогда не делитесь этим ключом
- Используйте разные ключи для Production и Development
- Храните ключ в безопасном месте

---

## Переменная 4: NODE_ENV (опционально)

**Key:** `NODE_ENV`

**Value:** `production`

**Окружения:** ✅ Production

**Примечание:** Vercel автоматически устанавливает это для production деплоев, но можно добавить явно.

---

## Пример заполнения в Vercel Dashboard

После добавления всех переменных, вы должны увидеть список:

```
┌─────────────────┬──────────────────────────────────────────────┬─────────────┐
│ Key             │ Value                                        │ Environments│
├─────────────────┼──────────────────────────────────────────────┼─────────────┤
│ DATABASE_URL    │ postgresql://user:pass@host:5432/db         │ Production  │
│                 │                                              │ Preview     │
├─────────────────┼──────────────────────────────────────────────┼─────────────┤
│ NEXTAUTH_URL    │ https://kongtrade-website.vercel.app        │ Production  │
│                 │                                              │ Preview     │
├─────────────────┼──────────────────────────────────────────────┼─────────────┤
│ NEXTAUTH_SECRET │ xK8pL2mN9qR5sT7vW3yZ6bC1dF4gH8jK0lM2nP5rS8t │ Production  │
│                 │                                              │ Preview     │
└─────────────────┴──────────────────────────────────────────────┴─────────────┘
```

---

## Проверка переменных

### Через Vercel Dashboard:
1. **Settings** → **Environment Variables**
2. Убедитесь, что все переменные добавлены
3. Проверьте, что выбраны правильные окружения

### Через Vercel CLI:

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Подключите проект
vercel link

# Просмотрите переменные окружения
vercel env ls
```

---

## После добавления переменных

1. **Сохраните все переменные** (нажмите Save для каждой)
2. **Передеплойте проект:**
   - Перейдите в **Deployments**
   - Найдите последний деплой
   - Нажмите три точки (⋮) → **Redeploy**
   - Или сделайте новый commit в GitHub (автоматический деплой)

---

## Важные замечания

### 🔒 Безопасность:
- Никогда не коммитьте переменные окружения в Git
- Используйте разные секреты для разных окружений
- Регулярно обновляйте `NEXTAUTH_SECRET`

### 🔄 Обновление переменных:
- После изменения переменных нужно передеплоить проект
- Изменения применяются только к новым деплоям

### 📝 Порядок действий:
1. ✅ Добавьте `NEXTAUTH_SECRET` (можно сразу)
2. ✅ Добавьте `NEXTAUTH_URL` (временное значение, обновите после деплоя)
3. ⏳ Настройте базу данных (Supabase/Neon/Vercel Postgres)
4. ✅ Добавьте `DATABASE_URL` (после настройки БД)
5. ✅ Примените миграции Prisma (после настройки БД)

---

## Решение проблем

### Проблема: Переменные не применяются

**Решение:**
- Убедитесь, что выбраны правильные окружения (Production/Preview)
- Передеплойте проект после добавления переменных
- Проверьте, что переменные не имеют лишних пробелов

### Проблема: DATABASE_URL не работает

**Решение:**
- Проверьте правильность строки подключения
- Убедитесь, что база данных доступна из интернета
- Проверьте firewall настройки базы данных
- Для Supabase: используйте **Connection pooling** строку для production

### Проблема: NEXTAUTH_URL неправильный

**Решение:**
- После первого деплоя скопируйте точный URL из Vercel Dashboard
- Обновите переменную и передеплойте
- После добавления кастомного домена обновите на `https://kongtrade.com`

---

## Быстрая команда для генерации NEXTAUTH_SECRET

```bash
# macOS/Linux
openssl rand -base64 32

# Windows (PowerShell)
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))

# Или через Node.js (любая ОС)
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

---

## Следующие шаги

После настройки всех переменных:

1. ✅ Передеплойте проект
2. ✅ Проверьте, что сайт загружается
3. ✅ Настройте базу данных (если еще не настроена)
4. ✅ Примените миграции Prisma:
   ```bash
   vercel env pull .env.local
   npx prisma migrate deploy
   ```
5. ✅ Проверьте работу API routes
6. ✅ Обновите `NEXTAUTH_URL` на кастомный домен (после настройки)

