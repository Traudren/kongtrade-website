
[build]
  command = "npm install --legacy-peer-deps && npx prisma generate && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

# Временно отключен плагин, так как требует NETLIFY_AUTH_TOKEN
# Для включения добавьте NETLIFY_AUTH_TOKEN в переменные окружения Netlify
# [[plugins]]
#   package = "@netlify/plugin-nextjs"
