{
  "buildCommand": "npm install --legacy-peer-deps && npx prisma generate && npx prisma migrate deploy && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "env": {
    "PRISMA_SCHEMA_DISABLE_ADVISORY_LOCK": "1"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}